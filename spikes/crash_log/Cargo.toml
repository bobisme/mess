[package]
name = "crash_log"
version = "0.1.0"
edition = "2024"

# Standalone spike: opt out of the repo's root workspace.
[workspace]

[dependencies]
crc32fast = "1"
rand = "0.8" # used by the FaultWriter crash simulation

[dev-dependencies]
tempfile = "3"
