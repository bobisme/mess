# Attempt 2: inconclusive fatal evidence

Classification: `INCONCLUSIVE_FATAL`

This is an immutable copy of the one canonical Attempt-2 output. It contains
136 accepted prepared-pipeline rows, 952 accepted exploratory rows, 440
accepted boundary rows, their complete manifests, provenance, correctness,
plan, candidate rules, raw log, and fatal sidecar. Selection and confirmation
never ran because the runner could not execute the mode-`0644` evaluator.

There was no retry, replacement, restart, manual evaluator invocation, or
third attempt. These rows are audit evidence only and cannot support a
performance verdict. See `../../REPORT.md` for the disposition and process
lesson.

`SHA256SUMS` records hashes calculated from the stopped external output and
verified after this copy.

