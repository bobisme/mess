[package]
name = "compression-spike"
version = "0.1.0"
edition = "2024"

# Standalone spike: opt out of the repo root workspace.
[workspace]

[dependencies]
zstd = "0.13"
rand = "0.9"
serde_json = "1"

[profile.release]
opt-level = 3
lto = "thin"
