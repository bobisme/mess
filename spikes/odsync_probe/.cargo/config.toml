# The repo root .cargo/config.toml formerly injected a hard lld requirement;
# this local override neutralizes it for this spike (see spikes/README.md).
# Kept for parity with the other spikes even though the root override is gone.
[target.x86_64-unknown-linux-gnu]
linker = "/usr/bin/clang"
